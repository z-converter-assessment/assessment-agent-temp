# C build artifacts
src/*.o
/assessment-agent
/assessment-agent-legacy
windows-agent/src/*.o
windows-agent/src/*.res
windows-agent/*.exe
windows-agent/*.exe.manifest
windows-agent/build-deps.txt

# Vendored third-party sources (fetched by `make vendor-fetch`)
/vendor/
/windows-agent/vendor/

# build staging + release artifacts (regenerated by `make release`)
/build/
/dist/
/windows-agent/dist/

.env
.DS_Store
